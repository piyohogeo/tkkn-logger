# 『特訓'99 -君よ、男避けに避けろ-』自動ロガー開発 引き継ぎ文書

## 1. 文書の目的

本書は、Windows 上で動作する『特訓'99 -君よ、男避けに避けろ-』のプレイを監視し、ハイスコア動画と終了時メッセージを自動収集するロガーを Codex が実装するための引き継ぎ仕様である。

最初から完全自動プレイを目指さない。ユーザーが通常どおりゲームを操作し、ロガーは画面の観測・録画・スコア抽出・保存判定・メッセージ収集だけを担当する。

## 2. 確定要件

### 2.1 目的

1. 生存時間の歴代最高値を更新した run の動画を自動保存する。
2. 弾数の歴代最高値を更新した run の動画を自動保存する。
3. 生存時間に応じて表示される終了時メッセージを収集し、全種類のコンプリートを支援する。

ハイスコアは「生存時間」と「弾数」の独立した 2 系統で管理する。同じ run が両方を更新した場合、動画実体は 1 本でよく、両方の記録から同一ファイルを参照できるようにする。

### 2.2 明示的に行わないこと

- Enter キーを含むゲーム操作の自動化・キー送信・入力注入は行わない。
- 自動プレイ、回避操作、マクロ化は行わない。
- 弾密度などを使った第 3 のランキングは作らない。
- 初期実装では汎用 OCR を主方式にしない。
- 調査前に既存 Conda 環境へ大量のパッケージを追加しない。

入力注入機能は「無効にしておく」のではなく、原則としてコードベースに実装しない。`pyautogui` 等が別用途で導入済みでも、ゲームへのキー送信には使用しない。

## 3. ゲームの観測状態

画面状態は次の有限状態機械として扱う。

```text
TITLE --ユーザーがEnter--> PLAYING --被弾--> RESULT
RESULT --ユーザーがEnter--> MESSAGE --ユーザーがEnter--> TITLE
```

ロガーは遷移を画面から検出するだけであり、遷移を発生させない。

| 状態 | 画面の特徴 | ロガーの主処理 |
|---|---|---|
| `TITLE` | タイトル、開始案内 | 待機、ウィンドウ位置・サイズの監視 |
| `PLAYING` | 弾幕、自機、プレイ画面 | run 開始、フレーム録画 |
| `RESULT` | 失格表示、生存時間、弾数 | 録画継続、2 スコア抽出、保存判定 |
| `MESSAGE` | 終了時メッセージ | 正規化画像の採取、既知・未知判定、必要なら録画終端 |

状態が不明な場合は `UNKNOWN` とし、推測でスコア確定や状態遷移をしない。連続する複数フレームで一致したときだけ状態を確定するデバウンスを入れる。

## 4. 最初の必須工程：Anaconda / Python 環境の読み取り専用調査

### 4.1 現状

- OS: Windows
- 現在アクティブ: `py310_pt20`
- パス: `%USERPROFILE%\Anaconda3\envs\py310_pt20`
- 既知の他環境: `base`, `py310_mat`, `py310_pt20_clone20240621`, `py310_tf210`, `py310_webdriver`, `py312_pt24`, `py37_tf2`, `py37_webdriver`, `ros2`, `ros2_new`, `rp_pico_p310`, `rp_pico_py310`, `rp_pico_py39`, `test_py37` など

### 4.2 安全方針

最初の工程ではインストール、アップグレード、アンインストール、環境ファイルの書き換えを一切行わない。特に `pip install`、`conda install`、`conda update` は実行しない。まず調査結果と変更案をユーザーへ提示し、合意後に進める。

既存環境を壊さないため、次を守る。

- `base` は変更しない。
- `py310_pt20` の既存用途と依存関係を尊重する。
- `pip` は必ず対象 Python に紐づく `python -m pip` として扱い、別環境への誤インストールを防ぐ。
- 実際の Python 実行ファイル、`pip`、`conda` のパスを先に記録する。
- 依存変更前に `conda list --explicit` と `conda env export` 相当の復元情報を保存する。
- 新規専用環境を作る場合も、調査結果・環境名・Python バージョン・導入予定パッケージを先に提示する。
- GPU 対応 PyTorch 等、本ロガーに不要な既存スタックには触れない。

### 4.3 調査項目

Codex は `py310_pt20` を明示指定して、少なくとも以下を収集する。可能なら結果を `artifacts/env_audit/` 以下に保存し、秘密情報やユーザー固有の不要な環境変数は記録しない。

1. Windows のバージョンとアーキテクチャ
2. `where conda`, `conda --version`, `conda info`, `conda env list`
3. `py310_pt20` の `python --version`、実行ファイルパス、64/32 bit
4. `python -m pip --version`、`python -m pip list`
5. `conda list`、可能なら明示的パッケージ履歴
6. `ffmpeg`, `ffprobe` の有無、パス、バージョン、利用可能な H.264 エンコーダ
7. `opencv-python` / `opencv-contrib-python` / Conda 版 OpenCV の有無と `cv2.__version__`
8. 画面キャプチャ候補の有無: `dxcam`, `mss`、必要に応じて Windows Graphics Capture 系
9. Windows 連携候補の有無: `pywin32` (`win32gui`, `win32process`, `win32api`)
10. `numpy`, `Pillow`, `imagehash`, `scipy`, `pydantic` 等の有無
11. `tkkn.exe` のプロセス・ウィンドウを読み取り専用で列挙できるか
12. ゲーム表示のクライアント領域サイズ、DPI スケーリング、モニター構成、ウィンドウ移動時の座標取得可否

調査用スクリプトを作る場合は、パッケージの import 成否を個別に捕捉し、未導入をエラー終了ではなくレポートに記録する。`ffmpeg` は PATH 上だけでなく、Conda 環境や既知の安全なローカル配置も読み取り専用で確認する。

### 4.4 環境選択の判断基準

調査後、次のどちらかを提案し、ユーザーの確認を得る。

**A. `py310_pt20` を流用**

- 必要な依存がほぼ揃っている。
- 追加が少数かつ競合リスクが低い。
- 既存用途への影響を許容できる。

**B. 専用環境を新規作成（一般にはこちらが安全）**

- 複数の新規依存が必要。
- OpenCV / NumPy / キャプチャライブラリの版競合があり得る。
- 長期運用時の再現性と保守性を優先する。

専用環境名の例は `tokkun99_logger`。ただし、勝手に作成しない。Python 3.10 を第一候補とし、必要最小限の依存だけを固定した `environment.yml` または `requirements-lock.txt` を用意する。環境作成後も `py310_pt20` は変更しない。

### 4.5 環境調査工程の成果物

- `artifacts/env_audit/summary.md`: 人が読める結果、問題、推奨案
- `artifacts/env_audit/conda_env_list.txt`
- `artifacts/env_audit/py310_pt20_conda_list.txt`
- `artifacts/env_audit/py310_pt20_pip_list.txt`
- `artifacts/env_audit/ffmpeg_report.txt`
- `artifacts/env_audit/capture_probe.json`
- 導入が必要な場合のみ、変更前の依存案と理由

この時点で一度停止し、「何が既にあり、何を追加する必要があり、どの環境を使うか」を提示する。ユーザー確認前に環境を変更しない。

## 5. 推奨アーキテクチャ

処理を次の責務に分離する。

```text
WindowLocator
  -> FrameCapture
  -> StateDetector
       -> RunRecorder
       -> ResultReader
       -> MessageCollector
  -> RunFinalizer
       -> SQLite/JSONL log
       -> record video retention/deletion
```

- `WindowLocator`: `tkkn.exe` のトップレベルウィンドウとクライアント領域を特定する。
- `FrameCapture`: クライアント領域を一定 FPS で取り込む。ゲームが前面でなくても取得できる方式が理想だが、黒画面等になる方式は避ける。
- `StateDetector`: 固定 ROI とテンプレート照合で状態を分類する。
- `RunRecorder`: `PLAYING` 検出からフレームを動画化し、`RESULT`、必要なら `MESSAGE` まで保持する。
- `ResultReader`: `RESULT` の固定 ROI から生存時間と弾数を読む。
- `MessageCollector`: メッセージ領域の正規化画像・ハッシュ・代表画像を管理する。
- `RunFinalizer`: run の整合性確認、記録更新判定、永続化、動画の正式保存または破棄を行う。

各モジュールは保存済み画像・動画でも動作できるようにし、ゲームを毎回起動しなくても再現テスト可能にする。

## 6. キャプチャと録画

### 6.1 キャプチャ方式の選定

実機プローブで次の順に評価する。

1. `dxcam`: Windows / Desktop Duplication で十分な速度と安定性が得られる場合
2. `mss`: 導入と扱いが単純で、前面表示運用で問題ない場合
3. Windows Graphics Capture 等: ウィンドウ単位取得が必要で、上記に問題がある場合

選定基準は FPS だけでなく、色再現、DPI、ウィンドウ移動、最小化、他ウィンドウによる遮蔽、ゲームの描画方式との相性で判断する。最小化時にキャプチャ不能なら、その制約を UI / README に明記する。

### 6.2 録画開始・終了

- 通常モード: `PLAYING` を確定した時点で run を開始する。
- 冒頭欠け対策: 数秒分のフレームをメモリ上のリングバッファに保持し、`PLAYING` 確定時にプレロールとして動画へ含める。
- `RESULT` 確定後も、結果画面が読める時間まで録画する。
- メッセージも動画に残す設定では `MESSAGE` の安定検出まで継続する。
- 状態検出失敗やゲーム終了時は動画を即削除せず、`incomplete` として隔離できるようにする。

FFmpeg が利用可能なら、Python から raw frame を pipe するか一時動画を生成し、H.264 MP4 に確定する。プロセス異常終了時に壊れた MP4 だけが残らないよう、`.partial` または一時ディレクトリから原子的にリネームする。FFmpeg 非搭載環境では OpenCV の `VideoWriter` も比較対象にするが、コーデック可用性を実機検証する。

### 6.3 保存ポリシー

設定で次を切り替えられるようにする。

- `records_only`: 2 系統のどちらかを更新した run だけ正式保存
- `collect_all`: 初期データ収集期間は全 run を保存
- `collect_samples`: 新しい状態画像、未認識文字、未知メッセージ、低信頼スコア等の必要サンプルだけ保存

平常運用の既定は `records_only`。ただし初期キャリブレーション完了までは `collect_all` または `collect_samples` を使用する。

同値を「更新」とするかは設定可能にしてもよいが、既定は厳密な `>` とし、歴代最高値を超えた場合だけ記録更新とする。

## 7. 状態検出とスコア抽出

### 7.1 固定 ROI 優先

ゲームの内部解像度、ウィンドウのクライアント領域、DPI を取得し、基準座標系へ正規化する。状態ごとに特徴的な小領域を複数用意し、テンプレートマッチング、二値画像差分、知覚ハッシュ等を組み合わせる。

単一ピクセルや単一 ROI だけに依存せず、複数の根拠と連続フレームで状態を確定する。閾値と一致度はログへ残す。

### 7.2 RESULT 読み取り

`RESULT` 画面の固定位置から次を抽出する。

- 生存時間: 表示値を可能なら整数ミリ秒へ正規化して保存
- 弾数: 整数

最初はフォント固定を前提に、数字 `0`～`9` と小数点のテンプレートマッチングを使う。文字分割が安定する場合は連結成分・固定ピッチ・列投影でグリフを切り出す。同じ数字でも描画位置や周辺画素の影響がある場合は複数テンプレートを許容する。

汎用 OCR は、テンプレート方式で吸収できない描画変動が実測された場合だけ比較検討する。OCR を使う場合も値域・書式・再読みによる検証を必須とし、低信頼値を自動で記録更新に使わない。

### 7.3 誤更新防止

次の条件を満たすまでスコアを確定しない。

- `RESULT` が複数フレームで安定している。
- 複数フレームからの読取値が一致する、または多数決で一意になる。
- 書式と値域が妥当である。
- 認識信頼度が閾値以上である。

不一致・低信頼・未知グリフ時は `needs_review` にし、候補値、ROI 画像、元フレームを保存する。人手確認前に歴代最高値を書き換えない。

## 8. MESSAGE の自動収集

### 8.1 基本方式

OCR で本文を直接確定せず、メッセージ領域の画像同一性で種類を管理する。

正規化の例:

1. クライアント領域から固定のメッセージ ROI を切り出す。
2. グレースケール化する。
3. 背景差分または閾値処理で文字画素を抽出する。
4. 不要な余白を切り詰める。ただし複数行・表示位置が意味を持つ場合は相対配置を保持する。
5. 必要に応じてごく小さな描画揺れを吸収する。
6. 正規化 PNG の SHA-256 を厳密 ID として保存する。
7. 知覚ハッシュまたは画素距離を近似クラスタリング補助に使う。

SHA-256 だけでは 1 ピクセル差で別物になるため、`exact_hash` と `perceptual_hash` / 類似度を併用する。ただし自動マージで別メッセージを失わないよう、曖昧な近似一致は `candidate cluster` として人手確認に回す。

### 8.2 人手ラベル

新規クラスタには採番 ID を与え、代表画像を保存する。後から次を編集できるようにする。

- 表示テキスト
- 注記
- 確認済みフラグ
- 別クラスタとの統合先

未知メッセージ検出時は run と生存時間を紐づける。収集が進んだら各メッセージの観測生存時間範囲、件数、境界候補を集計し、未発見帯を探せるようにする。生存時間からメッセージを先に決めつけず、観測データとして推定する。

### 8.3 コンプリート判定

ゲーム内部の全種類数が外部情報から確定しない限り、「未知メッセージが一定期間出ていない」だけで完全制覇とは断定しない。ツール上は次を区別する。

- 観測済みユニーククラスタ数
- 人手確認済みメッセージ数
- 生存時間帯の観測範囲と空白
- ユーザーが設定した期待総数に対する進捗

## 9. 初期キャリブレーションとデータ収集

手作業を減らすため、対話的な `calibrate` / `collect` モードを用意する。ただしゲーム操作は常にユーザーが行う。

### 9.1 自動プローブ

1. `tkkn.exe` のウィンドウを検出する。
2. プロセス名、ウィンドウタイトル、クライアントサイズ、DPI、座標を記録する。
3. 数秒間のキャプチャ試験を行い、FPS、フレーム重複率、黒画面率、解像度、色を報告する。
4. 候補キャプチャ方式が複数あれば同条件で比較する。
5. ユーザーへ最適方式と運用制約を提示する。

### 9.2 状態サンプル収集

収集ツールは現在の状態候補を画面表示またはログ表示し、ユーザーが通常操作で `TITLE`、`PLAYING`、`RESULT`、`MESSAGE` を順に見せる間、自動でフレーム群を保存する。

- 各状態で連続フレームを複数保存
- 状態遷移前後を時刻付きで保存
- ウィンドウ座標・サイズ・DPI をメタデータ化
- 画面全体に加えて候補 ROI を保存
- 個人情報を含むデスクトップ全体ではなく、可能な限りゲーム領域だけを保存

最初はラベル未確定の時系列として採取し、ユーザーが状態ラベルを確認できる簡単なマニフェストを生成する。Enter の押下検出や送信は不要で、画面変化だけを見る。

### 9.3 数字テンプレートの半自動作成

複数 run の RESULT ROI を保存し、連結成分や固定ピッチでグリフ候補を自動切り出す。候補を画像一覧にまとめ、人が `0`～`9`, `.` を一度ラベル付けできるようにする。

不足数字は「未収集」として可視化し、以後の run から新しい形状を自動追加候補にする。誤ラベルが歴代記録を汚染しないよう、テンプレートセットに版番号を付け、元 ROI と再解析可能にする。

既知の表示書式やユーザー確認値を利用できる場合は、文字列全体とグリフ列の対応を半自動で割り当てる。ただし確信できない割り当ては確定しない。

### 9.4 メッセージ初期収集

初期期間は `collect_all` を使い、各 run の MESSAGE 代表画像とスコアを保存する。正規化パラメータを複数候補で比較し、同一メッセージの描画揺れが同じクラスタになり、異なる文面が分離されることを人が確認してから固定する。

## 10. データ形式

### 10.1 推奨: SQLite を正本、JSONL を監査・交換用

SQLite はトランザクション、検索、2 系統ランキング、メッセージとの関連付けに適する。JSONL はデバッグ、差分確認、再処理用のイベントログとして任意で併用する。

#### `runs`

| 列 | 例 / 意味 |
|---|---|
| `run_id` | UUID |
| `started_at` / `ended_at` | ISO 8601、タイムゾーン付き |
| `survival_ms` | 整数。未確定なら NULL |
| `bullet_count` | 整数。未確定なら NULL |
| `score_confidence` | 0～1 または構造化評価 |
| `status` | `complete`, `needs_review`, `incomplete`, `error` |
| `is_survival_record` | 真偽値 |
| `is_bullet_record` | 真偽値 |
| `message_cluster_id` | 外部キー、未取得なら NULL |
| `video_path` | ルートからの相対パス、破棄時は NULL |
| `result_frame_path` | 相対パス |
| `capture_profile_id` | 使用設定 |
| `recognizer_version` | 再解析用の版 |
| `notes` | 任意 |

#### `message_clusters`

| 列 | 例 / 意味 |
|---|---|
| `message_cluster_id` | 安定した採番 ID |
| `exact_hash` | 正規化 PNG の SHA-256 |
| `perceptual_hash` | 類似検索用 |
| `representative_path` | 代表 PNG |
| `label_text` | 人手ラベル、未入力可 |
| `is_verified` | 人手確認済みか |
| `merged_into` | 統合先、通常 NULL |
| `first_seen_at` / `last_seen_at` | 観測日時 |
| `observation_count` | 観測回数 |

同じクラスタに複数の厳密ハッシュが入り得るため、実装時は `message_variants` テーブルに `exact_hash`, `cluster_id`, `sample_path`, `count` を分離するとよい。

#### `records_history`

| 列 | 意味 |
|---|---|
| `record_id` | 主キー |
| `metric` | `survival_ms` または `bullet_count` |
| `value` | 更新値 |
| `run_id` | 更新した run |
| `achieved_at` | 達成日時 |

現在値だけでなく更新履歴を残す。認識訂正が入った場合も監査可能にする。

#### `events`（任意）

状態遷移、信頼度、エラー、動画処理結果を append-only で記録する。大量のフレーム単位ログは通常無効にし、デバッグモードだけで有効にする。

### 10.2 整合性

- DB には絶対パスではなくデータルートからの相対パスを保存する。
- run 確定と記録更新履歴は同一トランザクションで行う。
- 動画確定に失敗した場合、記録フラグだけ成功扱いにしない。
- 動画・画像には必要に応じて SHA-256 を保存し、破損検出可能にする。
- スキーマには `schema_version` を持たせる。

## 11. 推奨ディレクトリ構成

```text
tokkun99-logger/
  README.md
  pyproject.toml                 # または最小限の依存定義
  config/
    default.toml
    local.toml.example
  src/tokkun99_logger/
    cli.py
    config.py
    window.py
    capture.py
    state_detector.py
    result_reader.py
    recorder.py
    message_collector.py
    storage.py
    models.py
  scripts/
    audit_environment.py
    calibrate_capture.py
    collect_samples.py
    label_glyphs.py
    label_messages.py
    replay_run.py
  tests/
    fixtures/
      states/
      results/
      messages/
    test_state_detector.py
    test_result_reader.py
    test_message_collector.py
    test_record_logic.py
    test_replay_pipeline.py
  data/                          # 原則 Git 管理外
    logger.sqlite3
    runs/
      YYYY/MM/DD/<run_id>/
        metadata.json
        result.png
        message.png
    videos/
      records/
      collection/
      incomplete/
    messages/
      clusters/
      variants/
    templates/
      states/<version>/
      glyphs/<version>/
    review_queue/
  artifacts/                    # 調査・校正レポート
    env_audit/
    calibration/
  logs/
```

ゲーム本体はプロジェクトへコピーせず、設定で実行中プロセスまたはユーザー指定パスを参照する。ゲーム配布物の再配布は行わない。

## 12. 設定項目の例

```toml
[target]
process_name = "tkkn.exe"
window_title_pattern = ""

[capture]
backend = "auto"
fps = 30
pre_roll_seconds = 2.0
include_message_in_video = true

[retention]
mode = "collect_all" # 初期収集後に records_only へ変更
keep_incomplete = true

[recognition]
state_stable_frames = 3
result_consensus_frames = 5
minimum_confidence = 0.98

[paths]
data_root = "data"
```

ROI 座標、基準解像度、テンプレート版、正規化閾値等はキャリブレーション成果物として別ファイルに持たせる。ユーザー固有の絶対パスを Git 管理対象の既定設定へ書かない。

## 13. 実装順序

### Phase 0: 安全な環境監査

1. 本書 4 章の読み取り専用調査を行う。
2. `summary.md` に結果と推奨環境をまとめる。
3. 必要な変更案を提示してユーザー確認を待つ。

### Phase 1: キャプチャ成立性の確認

1. 対象ウィンドウの安全な特定。
2. 候補方式で短時間キャプチャ。
3. 黒画面、DPI、座標ずれ、FPS、色を比較。
4. キャプチャプロファイルを確定。

### Phase 2: サンプル収集基盤

1. 時刻付きフレームとウィンドウメタデータの保存。
2. ユーザー操作中に 4 状態のサンプルを自動収集。
3. run 単位の暫定分割とレビュー用マニフェスト生成。

### Phase 3: 状態検出

1. 4 状態の ROI とテンプレートを作る。
2. `UNKNOWN`、連続フレーム確認、ヒステリシスを実装。
3. 保存済み時系列で遷移を再生テスト。

### Phase 4: RESULT 読み取り

1. RESULT ROI の抽出。
2. 数字・小数点グリフ候補の半自動収集とラベル。
3. 生存時間・弾数のパーサ、複数フレーム合意、信頼度を実装。
4. 低信頼時のレビューキューを実装。

### Phase 5: 録画と run 管理

1. プレロール付き録画。
2. 一時ファイル、安全な確定、異常終了回復。
3. SQLite スキーマと run トランザクション。
4. 生存時間・弾数の独立した記録更新ロジック。
5. `collect_all` / `collect_samples` / `records_only` を実装。

### Phase 6: メッセージ収集

1. MESSAGE ROI 正規化。
2. 厳密ハッシュ、近似候補、代表画像保存。
3. 未知クラスタ検出と run 紐付け。
4. 人手ラベル・統合用の簡易 CLI またはローカル UI。
5. 生存時間帯ごとの観測集計。

### Phase 7: 統合・長時間運転

1. 保存済み素材の全パイプライン再生。
2. 実ゲームで複数 run を監視。
3. ウィンドウ移動、取り逃し、低 FPS、突然終了を試験。
4. 初期収集モードから通常モードへ移行。

## 14. テスト戦略

### 14.1 単体テスト

- 各状態の陽性・陰性・境界サンプルで状態判定を検証する。
- 全数字、小数点、桁数違いを RESULT リーダーで検証する。
- 閾値近辺、ノイズ、1 ピクセルずれ、色差、整数倍率を試す。
- メッセージ正規化が同一画面の微小差を吸収することを確認する。
- 異なるメッセージが誤統合されないことを確認する。
- 2 系統の片方のみ更新、両方更新、どちらも非更新、同値を検証する。

### 14.2 リプレイ統合テスト

保存済みフレーム列または動画を入力し、実時間キャプチャなしで以下を再現する。

- `TITLE -> PLAYING -> RESULT -> MESSAGE -> TITLE`
- run の開始・終了時刻
- スコア抽出
- メッセージクラスタ割り当て
- 動画保存判定
- DB レコードとファイルの整合性

同じ素材を何度処理しても重複 run や二重の記録更新を起こさない、または明示した新規セッションとして扱う、という冪等性方針を決める。

### 14.3 実機テスト

- 通常プレイを複数回行う。
- ゲームウィンドウを移動する。
- DPI の異なるモニターへ移動する（対応範囲なら）。
- 一時的な遮蔽・フォーカス喪失を試す。
- ロガーまたはゲームを途中終了する。
- ディスク容量不足に近い状態は安全に模擬し、録画暴走を防ぐ。

### 14.4 ゴールデンデータ

人が確認した RESULT 画像と期待値、MESSAGE 画像とクラスタを小さなゴールデンセットとして固定する。テンプレートや閾値変更時は必ず回帰テストする。実プレイ動画やゲーム画像をリポジトリへ含める際は権利・容量・公開範囲に注意し、必要ならローカル専用 fixture とする。

## 15. 障害時と運用上の注意

- 対象ウィンドウが複数ある場合は自動選択せず、PID とタイトルを提示する。
- ウィンドウ消失時は run を `incomplete` として閉じ、録画を隔離する。
- スコア認識失敗時は動画・RESULT ROI を保持し、後で再解析できるようにする。
- DB と動画保存の片方だけが成功しないよう、確定順序と回復処理を設計する。
- `collect_all` の放置による容量増大を避け、空き容量警告・上限・保持日数を設定可能にする。上限超過時も記録動画を勝手に消さない。
- ログにスクリーン全体、ユーザー名、不要な絶対パス、環境変数を過剰に記録しない。
- 認識器を更新したら過去の RESULT ROI を再解析できるコマンドを用意し、訂正履歴を残す。

## 16. 最低限の操作インターフェース

CLI の例:

```text
tokkun99 audit-env          # 読み取り専用の環境調査
tokkun99 probe-capture      # 短時間のキャプチャ成立性確認
tokkun99 calibrate          # 状態・ROI・数字サンプル収集
tokkun99 run                # 通常監視
tokkun99 replay <path>      # 保存素材を再処理
tokkun99 review scores      # 低信頼スコア確認
tokkun99 review messages    # 未ラベルメッセージ確認
tokkun99 stats              # 2 系統の最高値と収集進捗
```

CLI 名は実装に合わせて変更してよい。重要なのは、環境監査、キャリブレーション、通常運用、再解析、レビューを分離することである。通常監視にゲーム操作機能は含めない。

## 17. 完了条件

### 17.1 環境・安全

- 読み取り専用の環境監査が完了し、結果と環境選択理由が文書化されている。
- ユーザー確認前に既存 Conda 環境を変更していない。
- 採用環境が再現可能な依存定義を持つ。
- ロガーにゲームへの Enter 送信・入力注入機能がない。

### 17.2 機能

- 4 状態と遷移を実プレイで安定検出できる。
- RESULT から生存時間と弾数を所定精度で取得できる。
- 低信頼値が自動でハイスコアを更新しない。
- 生存時間・弾数を独立に比較し、更新 run の動画を保存できる。
- 両方更新時に同一動画を重複保存せず参照できる。
- 非更新動画は通常モードで破棄され、初期収集モードでは保持できる。
- 未知の MESSAGE を自動採取し、既知候補と区別し、人が後からラベルできる。
- run、スコア、動画、メッセージの対応を SQLite から追跡できる。

### 17.3 品質

- 保存済み素材だけで主要処理を再現できる。
- 主要な単体テストとリプレイ統合テストが通る。
- ゲームやロガーの突然終了後も DB と一時動画を回復・隔離できる。
- README に起動手順、対応する表示条件、既知の制約、データ保存先、容量管理、バックアップ方法がある。

「終了時メッセージを完全制覇した」という最終判断は、期待総数の根拠が判明した場合はその総数との一致で行う。総数が不明な間は、観測済み・人手確認済み・未観測の生存時間帯を報告し、断定しない。

## 18. Codex が最初に行うこと

1. リポジトリ内の既存ファイル、`README`、`AGENTS.md`、Git 状態を読み取り専用で確認する。
2. 本書 4 章の環境監査スクリプトと出力先だけを実装する。
3. `py310_pt20` を明示して読み取り専用監査を実行する。
4. 結果を `artifacts/env_audit/summary.md` にまとめる。
5. `py310_pt20` 流用案と専用環境案を、追加依存・競合リスク・再現性の観点で比較する。
6. 推奨案と正確な変更予定をユーザーへ提示し、承認を待つ。
7. 承認後にのみ環境作成または必要最小限の依存導入へ進む。

この順序を飛ばしてキャプチャライブラリや OpenCV を既存環境へ追加しないこと。
